# Supabase share-code sync (frontend). Both are safe to expose:
# RLS on the `progress` table scopes anon read/write to the matching share_code.
VITE_SUPABASE_URL=https://YOUR-PROJECT-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key
